- name: Filters
  hosts: local
  connection: local
  tasks:
  - name: print the message
    ansible.builtin.debug:
      msg: " Hello {{ name | default('charan')}} "
  
  - name: convert to upper case
    print: "Hello charan"
    ansible.builtin.debug:
     msg: " {{ print | upper}} "

  - name: convert to upper case
    print: "Hello charan"
    ansible.builtin.debug:
     msg: " {{ print | lower}} "